#pragma once

#include <cstdint>

inline constexpr const char* AppName = "${PROJECT_NAME}";
inline constexpr const char* AppVersion = "${VERSION_MAJOR}.${VERSION_MINOR}.${VERSION_PATCH}-${VERSION_TAG_UNSTR}";
inline constexpr uint32_t VersionMajor = ${VERSION_MAJOR};
inline constexpr uint32_t VersionMinor = ${VERSION_MINOR};
inline constexpr uint32_t VersionPatch = ${VERSION_PATCH};
inline constexpr const char* VersionTag = "${VERSION_TAG_UNSTR}";
inline constexpr uint32_t VersionCommitNum = ${GIT_NUM_SINCE_TAGGED};
inline constexpr bool VersionDirty = ${VERSION_DIRTY};
inline constexpr uint32_t VersionGithashInt = 0x${GIT_COMMIT8};